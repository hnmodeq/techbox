import { privatePageMetadata } from "@/lib/seo";

export const metadata = privatePageMetadata("تکمیل خرید | تکباکس", "تسویه‌حساب امن فروشگاه تکباکس");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
