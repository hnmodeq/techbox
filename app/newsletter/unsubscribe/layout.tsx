import { privatePageMetadata } from "@/lib/seo";

export const metadata = privatePageMetadata("لغو عضویت خبرنامه | تکباکس", "مدیریت خصوصی عضویت خبرنامه");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
