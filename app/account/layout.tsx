import { privatePageMetadata } from "@/lib/seo";

export const metadata = privatePageMetadata("حساب کاربری | تکباکس", "مدیریت حساب کاربری تکباکس");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
