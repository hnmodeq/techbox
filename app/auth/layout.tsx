import { privatePageMetadata } from "@/lib/seo";

export const metadata = privatePageMetadata("ورود و امنیت حساب | تکباکس", "صفحات احراز هویت تکباکس");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
