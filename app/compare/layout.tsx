import { privatePageMetadata } from "@/lib/seo";

export const metadata = privatePageMetadata("مقایسه محصولات | تکباکس", "مقایسه انتخاب‌های شخصی محصولات");

export default function Layout({ children }: { children: React.ReactNode }) {
  return children;
}
