in media page (/media):
i want to do a full re-design in this page.
1-the whole page gonna be spread in 2 vertical sections, want to do a redesign
2-the first section is on the right side of the page, its a vertical gallery which we gonna have all of our reels in 1 column after eachother
3-the second section is on the left side of the page, it's a video player. the player play the latest video once a user enter the page. also if he choose a video from the right side the video player gonna play it.
4-in the left side we gonna have a button top of the video player as " previous video" and a button below of the video player as "next video"
5-when the video is going to next it needs a transiton to move the currenct video to up and out of the page. and the next video gonna move up from out of the page bottom to sit in center and play the video. it is just like what the youtube shorts is working. this mechanism gonna be reverse if someone click on previous buttn.
6-about the title/caption/comment/like section of the video let it be just like what it is (check the video modal, when you click on a video we have a pop-up modal open with 2 side, the right side is the video and the left side is the title/caption/comment/like section) in this situation we can reverse the video modal (the left side be the title/caption/comment/like section and the right side is the video player auto play)



in raid calculator page:
we have 3 section (1-select drives, 2-select raid type, 3-recomended products).
1-add a button as "بهترین ذخیره‌ساز بر اساس نیاز شما" which is lock and not clickable.
2-i want the second section have no default RAID (RAID 5 is the defaul, but let the user choose anything he wants without any default option)
3-when the user enter to the page he gonna see only step 1 and a locked button below the step 1 (which is the best recomended products depends on your needs)
4-when the user chose the RAID type the button gonna be unlock! when the user click on it the Step 3 (which is the recomended products) will be appears below step 2
5-when the step 3 appears the step 1 and step 2 will be locked and not clickable.
6-when the step 3 appears the "بهترین ذخیره ساز بر اساس نیاز شما" button well be change to "شروع دوباره"
7-move the یادداشت ها section to step 2 (to be part of step 2)
8-when step 3 appears the old "بازنشانی" button which is inside the step 1 can be invisible
9-when the step 3 appears the page can get scroll automatically to go on the step 3 section



# TASK: Comprehensive Bug Fix, Security Hardening, Performance Optimization & Feature Upgrade for TechBox (Next.js 16)
You are tasked with fixing all identified issues, security vulnerabilities, bundle bloat, SEO defects, and missing features in the `hnmodeq/techbox` codebase (Next.js 16 App Router, TypeScript, Prisma, Tailwind CSS).
Please execute the following step-by-step resolution plan cleanly and rigorously:
---

### 1. CRITICAL BUGS & UI BLANKS

1. **Fix Client Auth Desynchronization (`lib/auth.ts` & `lib/auth-server.ts`)**:
   - Replace persistent `localStorage.setItem('tb_auth_user', ...)` with server-validated session queries via `useSWR` or React Query hitting `/api/account/me`.
   - Prevent situations where expired HTTP-only cookies (`tb_session`) leave stale logged-in states in `localStorage`.

---

### 2. SECURITY HARDENING & DATA LEAK PREVENTION

1. **Eliminate Dev Fallback Auth Secret (`lib/auth-server.ts`)**:
   - Remove the hardcoded `dev-secret-please-change-32char!` fallback string.
   - Throw a fatal startup error if `AUTH_SECRET` environment variable is not defined or is less than 32 characters in length across all environments.

2. **Protect Confidential Resume Blob Files (`app/api/admin/jobs/applications/route.ts`)**:
   - Job applicant PDF resumes are uploaded directly as public Vercel Blob URLs (`*.public.blob.vercel-storage.com`), exposing personal contact details.
   - Refactor job application uploads to serve resumes through an authenticated API endpoint (`/api/admin/jobs/applications/[id]/resume`) that checks for admin authorization or secure signed tokens before streaming the file.

3. **Enforce Rate Limiting in Production (`lib/rate-limit.ts`)**:
   - Add a critical log warning in production builds if `UPSTASH_REDIS_REST_URL` and `UPSTASH_REDIS_REST_TOKEN` are missing, because serverless in-memory `Map` instances fail to protect against distributed brute-force attacks across Vercel function invocations.

---

### 3. PERFORMANCE & BUNDLE SIZE OPTIMIZATION

1. **Deduplicate Motion Animation Libraries (`package.json`)**:
   - The project installs both `framer-motion` (^12.42.2) and `motion` (^12.42.2), causing duplicate JS bundle bloat (~150KB).
   - Consolidate all animation imports across the project to `motion/react` or `framer-motion`.
   - Remove `framer-motion` from `package.json` dependencies and clean up unused imports.

2. **Lazy Load Heavy 3D & Animation Libraries (`components/effects/*`)**:
   - Three.js (`three`) and GSAP (`gsap`) are currently imported synchronously in visual effect components.
   - Wrap 3D canvas and particle effect components with `next/dynamic` (`{ ssr: false }`) so Three.js (~600KB) is split into a lazy chunk and loaded only when visible in the viewport.

---

### 4. SEO & ACCESSIBILITY

1. **Dynamic Canonical URLs (`lib/seo.ts`)**:
   - Update `siteUrl()` to dynamically handle incoming request headers or enforce `NEXT_PUBLIC_SITE_URL` to avoid hardcoded fallback canonicals pointing to the Vercel staging URL (`hnmodeq-techbox.vercel.app`) when custom domains are used.
2. **Meta & Structured Data**:
   - Ensure all public pages (`/blog/[slug]`, `/news/[slug]`, `/tools`, `/shop`) render valid OpenGraph images, Twitter cards, and `Article` / `WebSite` JSON-LD schemas.

---

### 5. UX & RTL PERSIAN MOBILE IMPROVEMENTS

1. **Add Mobile Bottom Navigation Bar (`components/layout/LayoutShell.tsx`)**:
   - Implement a fixed bottom navigation bar (`sm:hidden fixed bottom-0 left-0 right-0 z-50 bg-background/95 backdrop-blur border-t`) with touch-friendly icons (Home, News, Tools, Shop, Account) for mobile users in Persian RTL layout.

---

### 6. NEW FEATURE: INTERACTIVE IT TOOLS HUB

1. **Expand `/tools` Section (`app/tools/page.tsx`)**:
   - Build client-side interactive calculators for IT professionals:
     - **Subnet & CIDR Calculator**: IP range, netmask, usable host count, and broadcast address.
     - **RAID Storage Calculator**: Usable capacity, parity loss, and fault tolerance for RAID 0, 1, 5, 6, and 10.

---

### VERIFICATION & QUALITY CHECKS

1. Run `pnpm run typecheck` or `tsc --noEmit` to verify zero TypeScript errors.
2. Run `pnpm run lint` to ensure no ESLint warnings or missing dependency arrays in hooks.
3. Execute `pnpm run test` or `pnpm run test:smoke` to verify all end-to-end and unit tests pass cleanly.