"use client";

export default function HeroSection() {
  return (
    <section className="w-full max-w-full flex flex-col justify-center items-center px-4 py-10 text-center" dir="rtl">
      <h1 className="text-[length:var(--hero-font-size)] text-[var(--hero-font-color)] font-extrabold tracking-tight">
        تکباکس
      </h1>
    </section>
  );
}
